To run the application:
    Open Xampp and start the Apache and MySQL servers.

Open the the backend in a ternimal and run the following command:
    $ php artisan serve
    
Open the frontend in a ternimal and run the following command:
    $ npm run dev